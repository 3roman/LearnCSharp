<Window x:Class="MVVMDemo.View.WindowMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVMDemo.View"
        mc:Ignorable="d"
        Title="MVVMDemo" Height="200" Width="250">
    <Grid Margin="5" >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="2">
            <TextBlock Text="Name"/>
            <TextBox Text="{Binding student.Name}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="2">
            <TextBlock Text="Age"/>
            <TextBox Text="{Binding student.Age}"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="2">
            <TextBlock Text="Gender"/>
            <TextBox Text="{Binding student.Gender}"/>
        </StackPanel>
        <Button Grid.Row="3" Content="ReadStudentInfo" Command="{Binding ReadStudentInfo}"/>
    </Grid>
</Window>
