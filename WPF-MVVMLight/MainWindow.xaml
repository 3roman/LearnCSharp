<Window x:Class="WPF_MVVMLight.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_MVVMLight"
        mc:Ignorable="d"
        Title="MainWindow" Height="70" Width="300"
        DataContext="{Binding Main, Source ={StaticResource Locator}}"
        >

    
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding X }" Margin="3" MinWidth="50"/>
            <TextBlock Text="+" />
            <TextBox Text="{Binding Y}" Margin="3" MinWidth="50"/>
            <TextBlock Text="=" />
            <TextBox Text="{Binding Z }" Margin="3" MinWidth="50"/>

            <Button Content="Add" Command="{Binding AddCommand}" Margin="3" MinWidth="50"/>
        </StackPanel>
    </Grid>
</Window>
